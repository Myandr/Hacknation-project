# Frontend

Hier kommt deine Frontend-Anwendung (z. B. React, Vue, Svelte oder statisches HTML).

## Beispiel-Setup mit Vite (React)

```bash
npm create vite@latest . -- --template react
npm install
npm run dev
```
